stages:
  - deploy
  - monitor

# Dashboard → GitLab Pages (replaces deploy_dashboard.yml)
pages:
  stage: deploy
  image: node:20
  rules:
    - if: $CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE == "push"
    - if: $CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE == "web" && $INTELLIGENCE_CYCLE != "true"
  script:
    - cd dashboard
    - npm ci
    - npm run build
    - mkdir -p ../public
    - cp -r dist/* ../public/
  artifacts:
    paths:
      - public

# Daily monitoring agent (replaces monitor_intelligence.yml)
monitor:
  stage: monitor
  image: python:3.10
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
    - if: $CI_PIPELINE_SOURCE == "web" && $INTELLIGENCE_CYCLE == "true"
    - if: $CI_PIPELINE_SOURCE == "api" && $INTELLIGENCE_CYCLE == "true"
  script:
    - pip install --quiet -r requirements.txt
    - python monitor_agent.py
